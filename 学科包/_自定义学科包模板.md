# 学科包 · 自定义模板

> 复制本文件为 `<学科名>.md`，填完 6 个部分即可。填不出来的部分**先问学生**，不要瞎猜。

---

## 1. 学科基本信息

| 项 | 内容 |
|---|---|
| 学科名 | `<填>` |
| 适用场景 | `<填，如：乐器、健身、写作、软件操作、绘画…>` |
| 学习者的目标 | `<填，考试 / 作品 / 实用能力>` |
| 典型学习资料形态 | `<填，教材 / 视频 / 谱子 / 动作示范 / 软件文档>` |

---

## 2. 三维定义（**必须三个独立，不许合并**）

| 维度 | 名称 | 判什么 | 典型 ✅ 的样子 |
|---|---|---|---|
| ① | `<填，如"概念理解">` | `<填>` | `<填>` |
| ② | `<填，如"动作正确">` | `<填>` | `<填>` |
| ③ | `<填，如"姿势规范">` | `<填>` | `<填>` |

> **怎么想维度 ③**：问自己「这个学科里，什么是**结构性的、必须扣分**的错误？」
> 例：乐器 → 指法/节奏；健身 → 关节角度/呼吸配合；写作 → 结构/逻辑链；软件操作 → 操作路径/快捷键规范。
> **找不到 ③ 也要硬找一个** —— 没有规范维度的学科不存在，只是它叫别的名字。

---

## 3. 判据细则

### ❌ 结构性错误（必须扣分）

1. `<填>`
2. `<填>`
3. `<填>`
4. `<填>`
5. `<填>`

### ⚠️ 可豁免（不扣分、不逐处改）

1. `<填>`
2. `<填>`
3. `<填>`
4. `<填>`

> **提示**：可豁免项要写「手滑、笔误、格式偏好、等价的不同做法」这一类。
> 关键判据：**「这个错误会不会导致结果/效果不对？」** 会 → 结构性问题；不会 → 可豁免。

---

## 4. 高频问题清单（评估时重点盯）

1. `<填>`
2. `<填>`
3. `<填>`
4. `<填>`
5. `<填>`

> 这份清单**越用越准**：每次发现新的错误类型就追加（只增不改）。

---

## 5. 出题形态

| 轮次 | 题型 | 要点 |
|---|---|---|
| 第一轮 | **概念/认知题**（1–2 题） | `<填>` |
| 第二轮 | **动手/实操题**（2 题） | `<填，必须变化条件，不能照抄示例>` |
| 第三轮 | **综合题**（1 题） | `<填>` |

> **每轮只发 1–3 题，本轮全 ✅ 才发下一轮**（见 `协议/00_导师协议.md` 第 2 节）。

**本学科什么样的题才有意义**：`<填>`

**本学科禁止出什么题**：`<填，如：纯背诵题 / 无标准答案的题 / 需要超纲工具的题>`

---

## 6. 验证方式（怎么确认学生真会了）

1. `<填>`
2. `<填>`
3. `<填>`
4. `<填>`

> 通用原则：**「能复述」不算掌握，「换个场景还能做对」才算。**
> 本学科的「换个场景」具体是什么？`<填>`

---

## 7. 配套阅读写法

本学科的资料通常是什么结构？（章 / 单元 / 曲目 / 动作组 / 课时）

```markdown
### 📚 配套阅读
- 必读：<资料> <定位到最小单元>（课前 / 课后）
- 选读：<资料> <定位>（什么时候再回来看）
- 本课自测来源：<资料> <定位>
```

优先推荐什么来源？`<填>`
