# CONTEXT — 领域词汇表

> 本项目**只定义术语，不写实现**。
> 当两个词容易混用时，以本表为准。术语一旦在此定下，所有文档必须遵守。

---

## 核心角色

### 导师（Tutor）
读取 `AGENTS.md` 并按协议教学的 AI。**不是**某个具体软件 —— 任何能读文件夹的 AI 工具在打开本项目后都成为导师。

> 反例：「Trae 是导师」❌ —— Trae 只是导师的**载体**。

### 学生（Student）
使用本项目的人。协议中「学生」始终指**人类用户**，不指 AI。

### 载体（Agent Host）
运行导师的 AI 工具（Trae / ZCode / Cursor / CodeBuddy…）。
**载体可替换，协议不可替换** —— 这是本项目的核心不变式。

---

## 结构术语

### 契约（Contract）
根目录的 `AGENTS.md`。**唯一真相**。所有规则以它为准。

### 跳板（Pointer）
为特定载体生成的入口文件（`CLAUDE.md` / `CODEBUDDY.md` / `.trae/rules/*` …）。
内容只有一句「去读 `AGENTS.md`」。**由脚本生成，不许手写。**

> 判据：如果某个文件里**重复了**协议规则，它就不是跳板，是**漂移源** —— 应当删除。

### 框架（Framework）
`协议/` `学科包/` `模板/` `示例/` `教程/` `_tools/` `docs/`。
**只读**，由 `git pull` 更新，不被学生数据污染。

### 数据（Data）
`我的学习/` 与 `资料/`。**学生所有物**，已被 `.gitignore` 忽略。

> 判据：**框架 vs 数据**的边界 = 「`git pull` 会不会覆盖它」。

### 覆盖层（Overlay）
学生的自定义规则文件 `我的学习/我的规则.md`。**优先级高于框架默认**。

放在数据目录里，所以 `git pull` 永不冲突；但在 `.gitignore` 中被显式保留，
所以学生一打开就能看到。

> 判据：**改规则的正确位置是覆盖层，不是改源码。**
> 反例：直接改 `协议/00_导师协议.md` ❌ —— 会和 `git pull` 冲突，且规则漂移。

### 规则优先级（Rule Priority）
冲突时的判定顺序，从高到低：

```
1. 我的学习/我的规则.md        ← 覆盖层，最高
2. 学生当场说的话
3. 我的学习/00-学习档案.md 🚦   ← 状态，不是规则
4. 协议/00_导师协议.md          ← 框架默认
5. 学科包/<学科>.md
6. AGENTS.md 其余部分
```

> 覆盖层**没写**的 → 用框架默认；**写了**的 → 以覆盖层为准，即使与框架相反。

### 学科包（Subject Pack）
定义**某个学科的三维评估口径**的文件。例如「编程的维度 ③ 是语法可编译，英语的是表达规范」。

> 反例：「学科包是课程内容」❌ —— 学科包只定义**怎么评**，不定义**教什么**。

---

## 节奏术语

### 轮（Round）
一次「导师出题 → 学生作答 → 导师评估」的完整往返。**一轮只出 1–3 题。**

### 本轮（This Round）
当前正在评估的这一轮。评估只判**本轮**题目；本课是否掌握，要等所有轮次都 ✅。

> 反例：「评估本次全部题目」❌ —— 那是旧设计（一次发 5 题）。
> 现在一课分 2–3 轮，评估的粒度是**轮**，不是课。

### 轮次追加（Round Appending）
回答文档**初始只放第一轮题目**，上一轮全 ✅ 后才**追加**下一轮。

> 判据：文档里如果一次列全 5 题，学生等于看到了全部题目 —— 与「一轮 1–3 题」相矛盾。

---

## 评估术语

### 三维评估（Three-Dimension Assessment）
概念理解 / 逻辑正确 / 规范表达。三个维度**独立**评分，互不替代。

> **必须独立**：合并成「综合打分」会破坏「全 ✅ 才推进」的机制。

### 维度 ③（Dimension Three）
形式规范维度。**名称随学科变化**：编程叫「语法可编译」，英语叫「表达规范」，数学叫「推导规范」，文科叫「表述规范」，考试叫「答题规范」。
取值来自 `学科包/<学科>.md`。

### 掌握（Mastered）
**仅当三个适用维度全为 ✅** 时才成立。部分掌握（任一 ⚠️）与未掌握（任一 ❌）都不算。

### 结构性错误（Structural Error）
会导致结果错误或无法运行/无法成立的问题。**必须扣分**。
例：`main` 写在类外、时态系统性错误、量纲不匹配、论证缺论据。

### 可豁免笔误（Exemptable Slip）
手滑、格式偏好、等价的不同写法。**不扣分、不逐处修改**。
判据：**「这个错误会不会导致结果不对？」** 不会 → 可豁免。

### 唯一学习证据（Sole Evidence）
学生的回答文档 `01_学生回答.md`。是否掌握，**以它的「最终复评结果」为准**，优先级高于教学文档里的历史勾选与聊天记录。

---

## 流程术语

### 代改（Direct Fix）
小问题由导师**直接修改回答文档**，不让学生再答一轮。必须同时讲透三段式。
**边界**：只改「写法」，不改「答案的思考」—— 核心结论必须由学生产出。

### 三段式（Three-Part Explanation）
`① 你的原答案 → ② 错在哪、为什么（讲原理）→ ③ 我改成了什么`。
只回一句「已修改」**不合格**。

### 未掌握闭环（Not-Mastered Loop）
完整评估 → 教学引导 → 学生修改 → 重新读取 → 复评 → 记录结果。
**不通过就不发新课**。

### 落档（Filing）
按事件把状态写进指定位置。见 `协议/03_落档事件表.md`。

### 三处状态（Three State Locations）
🚦 当前交接状态 / 📊 掌握表 / ⏳ 待办表。
**状态只在这三处维护** —— 同一信息写多处是明确的反例。

### 交接状态（Handoff Status）
`我的学习/00-学习档案.md` 顶部的 🚦 表。**只写「当前这一课」**，是新会话的**唯一**任务来源。

### 摸底（Placement）
首次使用时的定位测试。**不是考试** —— 目的是定起点，不是打分、不贴标签。

### 出口标准（Exit Criterion）
某课「怎样才算过」的定义。写进课程路线，**先于内容**确定。

---

## 容易混淆的词（必须区分）

| 词 A | 词 B | 区别 |
|---|---|---|
| 导师 | 载体 | 导师是协议里的角色；载体是运行它的软件 |
| 框架 | 数据 | `git pull` 会不会覆盖 |
| 跳板 | 契约 | 跳板只指向；契约才定规则 |
| 学科包 | 课程路线 | 学科包定「怎么评」；路线定「教什么、什么顺序」 |
| 可豁免笔误 | 结构性错误 | 会不会导致结果不对 |
| 摸底 | 评估 | 摸底定起点；评估定掌握 |
| 掌握 | 完成 | 完成 = 交作业；掌握 = 三维全 ✅ |
| 交接状态 | 掌握表 | 交接状态写「当前这一课」；掌握表写「长期能力」 |
